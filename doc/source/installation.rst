============
Installation
============

At the command line::

    $ pip install electionbot

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv electionbot
    $ pip install electionbot
